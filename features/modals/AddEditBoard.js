import { html } from 'preact'
import { styled } from 'goober'

const AddEditTaskContainer = styled('div')``

const AddEditTask = () => {
    return html`<${AddEditTaskContainer}>Hello World<//>`
}

export default AddEditTask
