import { html } from 'preact'
import { styled } from 'goober'

const DeletePopupContainer = styled('div')``

const DeletePopup = () => {
    return html`<${DeletePopupContainer}><//>`
}

export default DeletePopup
